import { FormControl } from "@angular/forms";



export interface IInscription {
    id: string;
    name: string;
    userId: string;
    studentId: string;
    cursoId: string;
    inscriptedAt: Date;    
  }